export type CreateHospitalState = {
  message?: string;
  errors?: Record<string, string[]>;
  success?: boolean;
};

export type UpdateHospitalState = {
  message?: string;
  errors?: Record<string, string[]>;
  success?: boolean;
}; 