export type CreatePatientState = {
  message?: string;
  errors?: Record<string, string[]>;
  success?: boolean;
};

export type UpdatePatientState = {
  message?: string;
  errors?: Record<string, string[]>;
  success?: boolean;
}; 